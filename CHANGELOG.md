# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- **[GAP-08]**: GC safe-point awareness for long-running reads. `TxnKvClient` now validates every new transaction's start timestamp against the cluster's GC safe point and fails fast with the new typed, non-retryable `TxnAbortedByGcException` instead of letting a doomed transaction run until TiKV rejects it mid-read with "GC life time is shorter than transaction duration". The safe point is fetched from PD (`GetGCSafePoint`) through a new `PdClient::getGCSafePoint()` and held in a new `SafePointCache` with a bounded refresh interval (default **30 s**, configurable via `options['gcSafePointRefreshMs']`; must be `>= 1`) so validation adds at most one PD round trip per 30 s (during a safe-point RPC outage, every `begin()` degrades to one failed fetch plus a warning and proceeds fail-open); a PD fetch failure degrades to a warning log (fail-open — validation never invents a new hard failure mode). Validation is on by default and can be disabled with `options['gcSafePointValidation'] => false`. Long-running reads (large scan, report job, batch export) can now hold GC back legitimately: `PdClientInterface::updateServiceGCSafePoint()` registers a service safe point with a TTL, exposed as `TxnKvClient::holdGcSafePoint($safePoint, $ttlSeconds)` / `releaseGcSafePoint()` with a per-instance default service ID (returns `null` when the cluster's PD does not support service safe points). `TxnReader::get()`/`scan()`/`batchGet()` now handle `KeyError.abort` at all: the GC abort text maps to `TxnAbortedByGcException` and any other abort text surfaces as a base `TiKvException` carrying the server message — previously **every** abort fell through silently and a GC-passed `get()` returned `null` as if the key had never existed (data-invisible failure); `ErrorClassifier` classifies the new exception as fatal so no retry layer re-attempts a start timestamp GC has already passed. (#422)

- **[DOC-49 reworded]**: the workflow's **Release Gate** now documents the two release steps it previously skipped. CHANGELOG finalization is an explicit step: `[Unreleased]` entries move into a new `## [vX.Y.Z] - YYYY-MM-DD` section (Keep a Changelog, semver, today's date), the empty `[Unreleased]` section stays on top for the next cycle, and the commit lands on master **before** `gh release create` tags it (the release is cut from the tag, so a changelog committed after tagging is not part of the release tree). The release-notes convention is written down as well: notes follow the structure of the existing tags (`v0.2.0` uses `### Added` + `### Removed` + `### Full Changelog`) — a one-line theme title, `### Added`/`### Changed`/`### Fixed`/`### Removed` sections summarising the CHANGELOG entries for humans (matching the dominant convention of the previous tags; the first release of a line may use free-form "What's New"), and a `compare/v<prev>...v<new>` link; step 8 now also states that every merged PR appends under `[Unreleased]`, never under a version heading. (#446)
- **[TEST-18]**: `Transaction::heartbeat()` failure paths left uncovered by #492 are now locked in by tests, against the **real** `RegionCache` wired to an `InMemoryMetrics` (issue #474 made `invalidate()` the single emission point, gated on an actual removal — so the tests assert metric emissions, not mock call counts, and a stale-mock harness cannot mask the gating): a top-level **NotLeader** region error on the `TxnHeartBeat` RPC is owned by `RetryExecutor::handleNotLeader()` — with a still-valid hint the cached region's leader is switched in place to the hinted store and the retry lands on the hinted leader's store address with **no** invalidation emitted; with a hint the cache cannot honour the region is dropped with reason `'not_leader'` and re-resolved from PD; a non-NotLeader region error (`RegionNotFound`) drops the region via `RegionErrorHandler::check()` (`'region_error'`) and the executor's pre-retry invalidation stays silent (its `getByKey()` gate misses — no double emission); and the wire contract — `TxnHeartBeatRequest` carries the advised TTL (10000) and `startTs`/`primaryLock` while `heartbeat()` returns the **granted** TTL the server echoed back, never the advised one. New `HeartbeatRegionErrorAndTtlTest` (companion to `HeartbeatKeyErrorTest`). (#338)
- **[DOC-51]**: `workflow.md` step 12 (merge) documents the `gh pr merge --squash --delete-branch` failure mode when the default branch is checked out in another linked git worktree — the merge succeeds but local branch deletion dies with `fatal: '<branch>' is already used by worktree at '<path>'` — with the manual `git push origin --delete` + `git worktree remove` workaround, and a pointer from the Quick Reference merge step. (#459)
- **[DOC-50]**: the Quick Reference "TTL not working" row in `docs/troubleshooting.md` now links to the [TTL Errors](docs/troubleshooting.md#ttl-errors) section and mentions the data-wipe-on-toggle caveat, matching the anchor-linking convention of the neighbouring rows. (#481)
- **[DOC-40]**: `docs/error-handling.md` now documents the four sites that throw the base `TiKvException` **directly** (no subclass): PD GetRegion fail-closed (`PD GetRegion returned no region for key`), the TSO failure path in `TimestampOracle::getTimestamp()` (which **re-wraps a `GrpcException`** into the base class — `catch (GrpcException)` will not match a transaction begin/commit timestamp failure), the missing-timestamp TSO response check, and the TxnHeartBeat key error reachable via `Transaction::heartbeat()`. A new table under the exception-hierarchy diagram lists each site with its verbatim, source-greppable message and a bare `catch (TiKvException $e)` note — a catch on any single subclass will not match these four. (#480)
- **[DOC-29]**: `docs/troubleshooting.md` now documents every exception class the client can raise — the ten previously undocumented ones (`DeadlockException`, `LockWaitTimeoutException`, `TransactionConflictException`, `TxnRetryableException`, `RetryBudgetExhaustedException`, `BatchDeadlineExceededException`, `BatchPartialFailureException`, `StoreNotFoundException`, `HealthCheckException`, `InvalidStateException`) plus `GrpcException` and `InvalidStoreAddressException`, each with the exact thrown message string (every quoted string is greppable verbatim in `src/`; the two TiKV server-side TTL strings are attributed to TiKV itself), what it means, the likely cause, the remedy and whether to retry, cross-linked to the full reference in `docs/error-handling.md`. A new "Transaction Failures" subsection covers write conflict, deadlock, lock-wait timeout and retryable-lock errors. The fabricated `RegionException: TTL not enabled` entry — an error string that exists nowhere in `src/` — is replaced with the real behaviour: a TTL `put()` against a cluster without `enable-ttl` fails with TiKV's own `ttl is not enabled, but get put request with ttl` surfaced as a `RegionException`, while `getKeyTTL()` on such a cluster trips a TiKV server-side panic ("get ttl on non-ttl store") that surfaces as a `GrpcException`; setup instructions now carry the data-wipe-on-toggle caveat. (#395)
- **[DOC-28]**: new `docs/error-handling.md` — a complete, source-verified error-handling reference. It documents all sixteen exception classes (the fifteen `TiKvException` subclasses plus `InvalidArgumentException`, which extends `\InvalidArgumentException` and sits explicitly outside the `TiKvException` hierarchy), with the class hierarchy as a diagram; a per-operation table listing one row for every public method of `RawKvClient` and `Transaction` with the exceptions each can raise (verified against method bodies, not just `@throws` annotations); an explicit caller-retryability verdict per exception (never retry / do not retry blindly / retry if idempotent / start a new transaction); precise `Transaction::commit()` double-apply semantics (commit timestamp reuse from #217, closed-state rejection); and a recommended production catch order. The incomplete three-arm catch example in `docs/operations.md` is replaced by a short sketch that links to the reference. (#394)

### Fixed
- `Transaction::heartbeat()` no longer discards the server's `KeyError` payload when the `TxnHeartBeat` RPC reports a key error — the payload was bound, null-checked, then thrown away behind the constant message `Heartbeat failed: key error`, so an operator debugging a long-running transaction (the primary heartbeat use case) could not tell whether the server had GC'd the transaction (`txn_not_found`), lost its lock (`txn_lock_not_found`), or reported a conflict. `retryable`/`abort` variants now surface as `TransactionConflictException` carrying the server's text, a `locked` response as `TxnRetryableException` (deliberately **not** resolved: the only lock a heartbeat can report is the calling transaction's own primary lock, and resolving it would roll the calling transaction back underneath itself), and every other variant falls back to a base `TiKvException` whose message names the variant via the new shared `KeyErrorDescriber` (extracted from `RegionErrorHandler`, so the region-error per-pair messages stay byte-identical). (#492)
- PHPUnit doc-comment metadata (`@dataProvider`, `@covers`) replaced with PHPUnit 11 attributes (`#[DataProvider]`, `#[CoversClass]`, `#[CoversMethod]`) in `GrpcResponseParserTest`, `ErrorClassifierTest`, `RegionRangeClipperTest`, `RegionCacheBinaryKeyTest` and `RegionResolverBinaryKeyTest`; `vendor/bin/phpunit --testsuite Unit --display-phpunit-deprecations` now reports 0 runner deprecations (was 6), keeping the suite ready for PHPUnit 12 where doc-comment metadata is removed. (#452)
- **[DOC-38 follow-up]**: `docs/advanced.md`, `docs/architecture.md` and `docs/configuration.md` no longer understate the retry wall-clock deadline (`retryDeadlineMs`, #294) as overshooting "by at most one backoff sleep". The deadline is checked only **before each attempt** (`RetryExecutor`, `$elapsedMs >= $this->deadlineMs` guard at the top of the loop), so a worker's real occupancy can exceed it by one final backoff sleep (**up to ~10 s under ServerIsBusy**) plus the full wall time of the last in-flight RPC — bounded only by the gRPC timeout option (`options['timeout']`), not by the retry deadline. All three files now carry the same corrected wording plus a worked example (`retryDeadlineMs => 1000` with a 5 s RPC ⇒ real occupancy ≈ 6 s + sleep) so operators tuning the deadline for PHP-FPM occupancy plan the true worst case. (#477)
- **[DOC-10 reworded]**: the documentation now states unambiguously that `batchScan` fans out its per-region scan RPCs **concurrently**, bounded by the `options['maxConcurrency']` cap (default **16**, must be `>= 1`) — the same cap that already governs `deleteRange`/`deletePrefix`/`checksum` and the SST store-mode switches (#295). The `maxConcurrency` option is now documented as a first-class client option in `docs/configuration.md`'s full-options example (previously it appeared only in `docs/error-handling.md`), with a performance note in the `docs/operations.md` Batch Scan section (mirroring the BatchGet/BatchPut notes) and inline comments in `docs/advanced.md`'s "Parallel Scanning" example clarifying which fan-out is capped. `batchGet`/`batchPut`/`batchDelete` also dispatch their per-region RPCs concurrently (uncapped — every send is issued before any wait, per the existing `docs/architecture.md` "Batch Concurrent Fan-Out" section); that behaviour is unchanged and explicitly not part of this cap. (#376)
- **[TEST-03 / CI-01]**: the batch fan-out test classes previously ran in **no CI job** — `BatchAsyncExecutorTest` (14 tests) and `CheckedGrpcFutureTest` (5 tests) called `requireGrpcExtension()` in `setUp()`, so the `unit-tests` job (which installs no `grpc` extension by design) silently skipped all 19, while the `grpc-unit-tests` job's `Grpc` testsuite was an explicit four-file list that did not include them. `SstIngestorTest` (12 tests) and `ParallelRangeOpsCapTest` (7 tests) had the same class of gap (skip-without-`--fail-on-skipped` in Unit; absent from Grpc). `src/Client/Batch/BatchAsyncExecutor.php` and `CheckedGrpcFuture.php` therefore had zero executed coverage in CI even though the tests existed. Fix: the four classes now run in the `Grpc` testsuite (where `ext-grpc` is present and the `--fail-on-skipped` gate is honest), the whole-class `setUp()` gates are replaced by per-method gates on exactly the tests that construct `\Grpc\Call` mocks, and the `ParallelRangeOpsCapTest` gate is removed entirely — it is pure PHP (drives the executor with `CheckedGrpcFuture::fromCallable`, never `\Grpc\Call`) and now also runs in the Unit suite without the extension. The `unit-tests` CI job now passes `--fail-on-skipped`, so a test silently skipping in that job fails the build instead of hiding (only remaining runtime skips are root-only filesystem checks in `TlsConfigBuilderTest`, which CI never triggers). (#323, #358, #359)
- **[TEST-15]**: `RegionErrorHandler::check()` now has full branch coverage: the three `expectNotToPerformAssertions()` placeholders are replaced with real assertions, the top-level `error` string path (`RawBatchPutResponse`/`RawBatchDeleteResponse`) is tested for both empty (no throw) and non-empty (throws `RegionException` with `BatchRequest`), and the per-pair `KeyError` path (`RawBatchGetResponse` → `KvPair::hasError()` → `describeKeyError()`) is covered via a `#[DataProvider]` for `retryable`, `abort`, `Locked` (with binary key `\x00\xffuser`), `Conflict`, `Deadlock` and empty/`null` `KeyError` (unknown error), plus ordering (first error wins) and the `null`-branch via reflection. `RegionErrorHandler.php` statement coverage rises from ~22% to >90%. (#335)
- **[DOC-39]**: the documentation now states that RawKV TTL mode and TxnKV are mutually exclusive. A TiKV cluster with `[storage] enable-ttl = true` runs in V1TTL storage mode, which serves RawKV with TTL but **not** transactional requests — a single cluster cannot serve both (TiKV's `APIVersion.V1TTL` accepts only raw requests). Previously every doc that recommended `enable-ttl=true` (README TTL section and TiKV Configuration example, `docs/operations.md` TTL Operations, `docs/configuration.md` deployment checklist) said nothing about the consequence for TxnKV, so a team following the TTL advice and later adopting TxnKV hit unexplained failures with no documented cause. The exclusivity note is now added at every `enable-ttl` recommendation site plus the `examples/ttl.php` docblock; the TxnKV section in `docs/getting-started.md` states the required cluster mode (default V1, `enable-ttl` unset) as a prerequisite; the two compose configurations are referenced as the worked example (RawKV E2E against `docker-compose.yml`, TxnKV E2E against `docker-compose.yml + docker-compose.txnkv.yml`); and `docs/troubleshooting.md` gains a "TxnKV Fails on a TTL-Enabled Cluster" entry plus a cheat-sheet row. The docs already carried the data-wipe-on-toggle caveat; changing the setting on a live cluster is an operational migration. (#405)
- **[DOC-26]**: `docs/advanced.md` no longer teaches a hand-rolled user-level retry loop (`withRetry()` + `isRetryable()`) around every client call. The client already retries all transient errors internally (`NotLeader`, `EpochNotMatch`, `ServerIsBusy`, `StaleCommand`, `RegionNotFound`, gRPC transport errors) through a bounded `RetryExecutor` — an outer loop multiplies the total wait (the inner executor can already spend up to 30 attempts / 30 s wall-clock on one operation) and can pin PHP-FPM workers far longer than either layer intends. The "Retry Strategies" chapter now opens with a "You do not need your own retry loop" warning, a budget table with the real defaults and source constants (30 attempts / 20 s cumulative backoff / 60 s ServerBusy budget / 30 s wall-clock deadline from #294), documents which budgets surface `RetryBudgetExhaustedException` (`attempts()`, `elapsedOrBackoffMs()`, `getPrevious()`) as a hard failure — attempt cap and deadline — versus the two sleep budgets whose exhaustion rethrows the original TiKV error, and links the full reference in `docs/configuration.md#retry-and-backoff`. (#392)
- **[OBS]**: `MetricsInterface::regionInvalidated()` is now emitted from every `RegionCache::invalidate()` drop path instead of a single one (TTL-expiry sweeps and LRU eviction intentionally do not count as invalidations), so dashboards built on `region_invalidations_total` no longer undercount. `RegionCache::invalidate()` is the single emission point: it accepts an optional `$reason` (default `'region_error'`), and emits exactly once per **actual** removal — invalidating an ID that is not cached emits nothing. Reasons: `'retry_region_error'` (RetryExecutor pre-retry invalidation — previously the only emitting site), `'not_leader'` (`RetryExecutor::handleNotLeader()` hint-unknown/no-hint drops — the sole owner of NotLeader drops at executor-owned call sites; the few txn call sites with no retry executor around them — commit()'s prewrite loop, `pessimisticLockBatch()`, `Transaction::batchGet()` — self-invalidate inline via `RegionErrorHandler::check(..., notLeaderOwnedByRetryExecutor: false)`, restoring master's stale-entry recovery), `'lock_resolve'` (`LockResolver::invalidateRegionFor()`), `'region_error'` (all other top-level region errors via `RegionErrorHandler::check()`). `RawKvClient::create()`/constructor and `TxnKvClient::create()`/constructor attach the configured metrics backend to the region cache automatically via the new in-place `RegionCache::attachMetricsIfAbsent()` (an explicitly attached backend wins; shared caches are mutated, never cloned or rebound); caches without a backend stay zero-cost (no-op). A NotLeader response with a still-valid hinted peer only switches the cached leader and emits nothing. The `MetricsInterface::regionInvalidated()` docblock now documents the real contract. (#474)
- **[DOC-11]**: `docs/advanced.md` no longer teaches a hand-rolled `TiKvMetrics` decorator around an invented `recordOperation()` method — that class does not exist in the library. The "Monitoring and Observability" chapter now documents the real, built-in observability hook: implementing `MetricsInterface` (six methods: `rpcStarted()`, `rpcCompleted()`, `retryAttempted()`, `regionCacheHit()`, `regionCacheMiss()`, `regionInvalidated()`) and registering it via the `options['metrics']` key on `RawKvClient::create()`. Includes a production-shaped Prometheus adapter example (label names declared at registration) and the shipped `InMemoryMetrics` for tests/benchmarks; full reference remains in `docs/configuration.md#metrics-and-observability`. (#377)
- **[LQ-01]**: the four `LockResolver` tests for the lock-resolution paths (`testResolveLockWithCommitTsGetsCommitted`, `testResolveLockWithZeroCommitTsRollsBack`, `testResolveLockWithActiveLockWaitsThenRollsBack`, `testResolveLockWithLockActionImmediatelyRollsBack`) now assert what the code actually does instead of only counting gRPC calls. The committed and rolled-back branches build the same `ResolveLockRequest` and issue the same `KvResolveLock` RPC; the only difference is one field — `setCommitVersion($commitTs)` versus `setCommitVersion(0)` — so swapping the two private resolve methods previously left all four tests green while silently inverting commit-versus-rollback behaviour on the transaction-recovery path (resolving a committed lock as rolled back destroys committed data; the reverse makes an aborted transaction's writes permanently visible). Each test now captures the `ResolveLockRequest` and asserts `getCommitVersion()` and `getStartVersion()`, and asserts the exact RPC method sequence (`KvCheckTxnStatus` → `KvCheckTxnStatus` → `KvResolveLock`) rather than just the call count. A swap of the two production methods now fails six tests. (#349)

### Changed
- **[PERF-08]** (#295): `RawKvRangeOps::deleteRange()`/`deletePrefix()`/`checksum()`, `RawKvScanner::batchScan()` and `SstIngestor::switchStoresMode()` now issue their per-region/per-store RPCs concurrently instead of serially. The wire sends are fanned out (all sends in a window are issued before any wait begins, so server-side latencies overlap), bounded by a concurrency cap — default **16** — configurable via `options['maxConcurrency']` on `RawKvClient::create()` (or the corresponding constructor argument; must be `>= 1`). Refactorings: a new `BatchAsyncExecutor::executeParallelCapped()` (windows of at most `$maxConcurrency` in-flight requests through the existing executor) and a new `GrpcClientInterface::callAsync()` (issue a unary send without waiting; response resolved later via `GrpcFuture`). `SstIngestor` dispatches every store's SwitchMode request before awaiting any. **Behaviour change:** a partial failure now surfaces as `BatchPartialFailureException` (aggregating every failing region) instead of aborting at the first failing region; `deleteRange`/`checksum` remain idempotent so retrying the whole operation is safe. (#295)
- **[PERF-07]**: the blocking retry backoff is now bounded by a wall-clock deadline by default, so a sustained `ServerIsBusy` episode can no longer pin a PHP-FPM worker for minutes inside a single request. `RawKvClient` and `Transaction` construct their `RetryExecutor` with a non-zero `deadlineMs` (default **30 s**, previously disabled — the only bounds were a 10-minute ServerBusy budget and the attempt cap, so one request could block for up to ~10 minutes). The deadline is configurable via the new `options['retryDeadlineMs']` on `RawKvClient::create()`/`TxnKvClient::create()` (and the corresponding constructor argument; `0` restores the old unbounded behaviour — not recommended under PHP-FPM), and is threaded into every retry executor the client builds, including the scan and range paths. The default `serverBusyBudgetMs` is reduced from 600 s to **60 s** (order-of-magnitude cut; it acts as a worker-occupancy setting, not a cluster-recovery horizon) and documented as such in `docs/configuration.md`. **Behaviour change:** operations that would previously have eventually succeeded after minutes of blocking backoff may now fail fast with `RetryBudgetExhaustedException`; this trade is intentional — bounded per-request latency and fast failure beat pool exhaustion during a TiKV degradation. (#294)

### Fixed
- **[CI]** (#485): the CI `Lint` gate is green again — rector 2.6.x (resolved fresh by CI's `composer install`; no lock file is committed) flagged eight files that the locally-pinned rector did not, failing every PR's lint. Six `src/Client` files are updated to the idiomatic `??=` null-coalescing-assign form (`IfToNullCoalescingAssignRector`), and rector is configured (`rector.php` `->withSkip()`) to leave two test classes alone because every transform rector 2.6 proposes for their `setUp()`-initialised properties breaks them: `ReadOnlyPropertyRector` + `RemoveUnusedPrivateMethodRector` deletes the `setUp()` that initialises the now-readonly props (leaving them uninitialised → "must not be accessed before initialization" errors in all 24 tests), and `PrivatizeFinalClassMethodRector` makes `setUp()` private, which PHPUnit rejects with a fatal "must be protected" error. Files: `RegionCache.php`, `RawKvBatch.php`, `SstIngestor.php`, `RegionGrouper.php`, `TwoPhaseCommitter.php`, `TxnReader.php` (mechanical `??=`), `rector.php` (skip list).
- `LockResolver::resolveLock()` no longer blocks for up to the full `maxBackoffMs` (20 s by default) on a still-active lock: the TTL wait sleep is now capped by an optional `$remainingDeadlineMs` parameter, and `TwoPhaseCommitter::pessimisticLockBatch()` passes its remaining pessimistic-lock budget into it and charges the wall time spent resolving (status RPCs + wait) back into `$elapsedMs`. Previously the lock wait was charged to no budget — invisible to both the pessimistic-lock budget (which could silently stretch past `maxBackoffMs`) and, when reached inside retry closures, to the RetryExecutor wall-clock deadline from #294. Callers that pass no remaining deadline keep the old uncapped-by-deadline behaviour; the read/scan/prewrite/rollback call sites are intentionally unchanged — their waits overshoot the #294 deadline by at most one resolve, detected at the next inter-attempt check. (#470)
- **[SEC-02]**: `TlsConfigBuilder` no longer silently reinterprets an unreadable certificate **path** as inline PEM content. The deprecated guessing methods (`withCaCert()`, `withClientCert()`) previously flipped from "read the file" to "treat the path string as the certificate body" whenever the file was missing or unreadable, so `TlsConfig::isEnabled()` still returned `true` and the client logged `tls: true` while gRPC could fall back to a plaintext channel — an invisible downgrade. A value that looks like a path (does not contain `-----BEGIN`) whose file does not exist or is not readable now throws `InvalidArgumentException` at construction time instead of connecting insecurely. `TlsConfig` now also rejects any certificate/key material that does not contain a PEM header. The docs and example now demonstrate the explicit `caCertFile`/`clientCertFile`/`caCertBaseDir` (and `*Pem`) options instead of the deprecated `caCert`/`clientCert` keys. (#305)
- **TEST-14 / CI-03**: fixed a failing `Integration` test and wired the suite into CI. `testRegionCacheInvalidationLogContainsRegionId` stubbed the second `grpc->call()` with a `RawGetResponse` while driving `RawKvClient::put()`, so the typed closure in `RawKvCrud::put()` threw a `TypeError`; the `Integration` suite (declared in `phpunit.xml`) was red with nobody running it. The stub now returns `RawPutResponse` as `put()` expects, and the test asserts the invalidation log carries both the `regionId` context field and the message identifying the reason (`Invalidated region on retry`). The previously-red `Integration` suite now exits 0 locally (`vendor/bin/phpunit --testsuite Integration`). A new `integration-tests` CI job runs the suite on every PR with `--fail-on-skipped`. (#334, #360)
- **[TEST-12]**: `RawKvScanner::validateScanLimit()` now rejects negative limits with `InvalidArgumentException('Scan limit must be 0 or greater')` before they reach `RawScanRequest::setLimit()`, a `uint32` protobuf field where `-1` serialises to `4294967295`. Previously a negative limit (from user input, a decremented counter, or an off-by-one) bypassed `MAX_SCAN_LIMIT` and became an effectively unbounded scan that could OOM the PHP worker and sustain heavy load on the TiKV cluster. The error message is now byte-identical to `TxnReader::normalizeScanLimit()`, so the two scan paths agree. Regression tests cover every public scan entry point (`scan`, `reverseScan`, `scanPrefix`, `batchScan`, `scanIterator`, `scanPrefixIterator` on `RawKvScanner`/`RawKvClient`, and `Transaction::scan`), the boundary values 1 / `MAX_SCAN_LIMIT` / `MAX_SCAN_LIMIT + 1`, and the cross-path message agreement. (#332)
- **GRPC-12 (retry)**: `RetryExecutor::execute()` now treats its backoff budget as **per-operation** — the `attempt`, `totalBackoffMs` and `serverBusyBackoffMs` counters were instance fields that never reset between calls, so a single executor reused across many operations (memoised in `Transaction::retryExecutor()`, once per region in `RawKvScanner::scan()`, once per sub-batch in `RawKvBatch`) silently carried its spent budget forward. Once the accumulated total crossed `maxBackoffMs`, every later operation failed on its first retryable error without retrying; a long transaction degraded into no-retry mode and large batches became progressively more fragile. The three counters are now per-invocation local state inside `execute()`, so every call starts with a full budget and `execute()` is reentrant — the executor can be reused safely. **Backward-compat note:** operations that previously failed early now retry as documented; overall latency under sustained cluster errors may increase, which is the intended behaviour (bounded by the attempt cap and per-call deadlines). Regression tests cover a reused executor retrying normally on its second call and a multi-region scan retrying with a fresh budget per region. (#271)
- **[GRPC-09]**: `GrpcClient::getChannel()` now throws `InvalidStateException` when called on a closed client, matching the guard already present in `call()`/`callStreaming()`. Previously a `getChannel()` after `close()` silently opened fresh channels and stored them in the never-again-drained channel map, leaking sockets and memory until `RLIMIT_NOFILE` — reachable via the `RawKvBatch` path or any retained `GrpcClient` reference. `close()` now drains the channel map unconditionally regardless of the internal closed flag, and `closeChannel()` is a no-op on a closed client, so a closed client can never hold a channel. **Backward-compat note:** code that called `getChannel()` after `close()` now throws instead of silently leaking; that code was already broken — the exception makes it visible. (#268)
- **[GRPC-07]**: `GrpcClient::call()` now starts its RPC latency timer **before** the blocking `startBatch()` send/receive calls and wraps channel acquisition, call construction and both batches in the `try`/`finally` so send-phase failures also record `rpcCompleted` (with `success=false`). Both `call()` and `callStreaming()` now use the monotonic clock `hrtime(true)` instead of wall-clock `microtime(true)` so NTP steps cannot produce negative durations, and both measure the same span. Previously `call()` assigned `$startMs` after the round-trip had already completed, so `MetricsInterface::rpcCompleted()` reported tens of microseconds regardless of actual network latency — dashboards and alerts built on it never reflected real TiKV latency. Reported latency values will now increase to their true scale; review alert thresholds on deploy. (#266)
- Region-boundary key routing now compares keys in **byte order** (`strcmp`), matching TiKV's region split semantics, instead of PHP's `<`/`<=`/`>=` string operators, which compared numeric-looking strings numerically. Numeric-string keys (user IDs, order IDs, epoch timestamps) can no longer be silently mis-routed to the wrong region — misplaced `batchPut`/`batchGet` writes, dropped keys in batches, and reversed/empty scan sub-ranges. Applies to `RegionCache`, `RegionResolver`, `RegionRangeClipper`, `RawKvBatch::keyInRegion` and `ScanIterator`. (#321)
- **[GRPC-08]**: `RawKvClient::scan()`/`reverseScan()` and `TxnKvClient::scan()` now re-resolve the region **inside** the retry closure instead of capturing a `RegionInfo` resolved before the retry loop. Previously, after `NotLeader` or `EpochNotMatch` the retry executor invalidated the cache and switched leader hints, but the closure re-sent the same stale region tuple, so every retry reproduced the original error until the attempt cap (up to 30 RPCs against a store that had already reported an error). Scans now pick up invalidation/leader switching like the single-key and batch paths already did. When a retry resolves a post-split region narrower than the requested sub-range, the scan **continues from the fresh region's end key** (forward) or prepends the higher remainder (reverse) so no part of the original range is silently dropped; the region cache is populated from the `scanRegions()` result on every scan so first attempts hit the cache. (#267)
- `make test-e2e` / `composer test:e2e` previously invoked a non-existent PHPUnit suite `E2E` (`phpunit.xml` defines `E2E-RawKV` and `E2E-TxnKV`), so the local E2E runner executed zero tests. `scripts/test-e2e.sh` now mirrors CI — RawKV E2E on the V1ttl cluster, then TxnKV E2E on the V1 cluster (txnkv compose override) — and `composer test:e2e` targets the existing RawKV suite. (#267)
- Numeric-string keys (`'12345'`, `'0'`) now work across TxnKV and RawKV read/write paths. Previously PHP's integer-like array-key coercion made `$txn->set((string) $userId, $payload)` throw a raw `TypeError` (uncatchable as `TiKvException`) at commit time — `TransactionState::getPrimaryKey()`/`getWriteKeys()` returned `int` keys — and `RawKvClient::batchPut(['12345' => 'v'])` failed validation the same way; transaction `batchGet()`/`scan()` were also affected. Keys are now string-cast at every typed consumption point (state accessors, prewrite mutations, batch region resolution, `KvPair`/`Pair` wire construction) and the batch APIs accept `string|int` keys, rejecting any other type with `InvalidArgumentException`. (#322)
- `Transaction::commit()` now throws a `LockWaitTimeoutException` (carrying the key and the backoff budget) when pessimistic lock acquisition exhausts its retry budget (`maxBackoffMs`), instead of silently continuing to prewrite without the locks — the transaction can no longer proceed/commit with lost or conflicting lock state. The exception message shows the key **redacted** via `KeyRedactor`; the raw key stays available through `getKey()`. (#219)
- **SEC-03 (security)**: `RegionResolver` validates every PD-supplied store address before it is used as a gRPC channel target — including the SST ingest `SwitchMode` calls (via the new public `RegionResolver::validateStoreAddress()`). Any address that is not a bare `host:port` (bracketed IPv6 `[addr]:port` accepted) is unconditionally rejected with a distinct `InvalidStoreAddressException` (logged at error level): `unix:`, `unix-abstract:`, `dns:///` schemes, a host that is itself a reserved gRPC/URI scheme name (`unix:20160`, `dns:20160`, `ipv4:20160`, `vsock:20160`, … — rejected case-insensitively so grpc-core never interprets the address as a URI at `new Channel()`), trailing-newline suffix tricks (anchored `\A…\z` regex), and out-of-range ports (0 or 65536+) can no longer reach the channel constructor. `InvalidStoreAddressException` is classified as fatal in `ErrorClassifier` before any message matching, so an address text containing retry keywords (e.g. `EpochNotMatch`) can no longer trigger up to 30 retries that hide the rejection. Prevents a rogue or on-path PD from redirecting the client — and the data it writes — to an arbitrary host, port or local Unix socket. (#306)
- **SEC-03 (security)**: when neither `allowedStoreHosts` nor `storeHostPolicy` is configured, the client now derives a default store-host policy from the **configured PD endpoints** instead of accepting any bare `host:port`. The host is classified **before** any rule runs: bracketed IPv6 literals are allowed only when byte-identical (`inet_pton`) to a configured PD IPv6 endpoint (zone-id forms like `[fe80::1%eth0]:20160` and IPv4-mapped forms like `[::ffff:10.0.0.1]:20160` are rejected — previously almost any bracketed IPv6 was trusted); IPv4 literals only when equal to a configured PD IPv4 literal or in its /16 subnet (IPs are compared by address bytes and never textually suffix-matched — previously `10.0.0.1` slipped past PD `127.0.0.1` via the shared `.0.1` suffix); digit-leading numeric-IP aliases (`2130706433`, `017700000001`, `0x7f000001`) are rejected outright; DNS names via exact match, single-label (compose/K8s short names such as `tikv1` next to `pd`), or shared last-two DNS labels. Anything else is rejected with a logged `InvalidStoreAddressException`, closing the rogue-PD TCP-redirect hole by default; an explicit `allowedStoreHosts`/`storeHostPolicy` still overrides the default. (#306)
- **SEC-03 (security, round 3)**: the shared last-two-DNS-label suffix rule now applies **only** when the configured PD host is a real dotted DNS name — PD endpoints that are IPv4/IPv6 literals (`inet_pton`), digit-leading numeric aliases (`123.456.789`) or single-label names no longer contribute a suffix, so with PD at `10.0.0.1:2379` the store address `attacker.0.1:20160` is rejected (previously accepted via the textual `.0.1` suffix). The reserved gRPC/URI scheme rejection list now also covers the resolver schemes registered by grpc-core 1.80 — `xds`, `google-c2p`, `google-c2p-experimental` (`xds:20160` etc. previously passed the format check and reached `new Channel()`). Store **ports** are now part of the trust decision: under the default PD-derived policy the address port must be `>= 1024` unless explicitly listed in the new `options['allowedStorePorts']`, and when that option is set the port must be in it (applies to every default-policy branch: exact PD host, /16, single-label, suffix). (#306)
- `LockResolver::checkTxnStatus()` no longer sends `hrtime()`-derived values as MVCC timestamps: `caller_start_ts` is now the resolving transaction's PD-allocated `start_ts` and `current_ts` is a freshly allocated PD TSO timestamp (fetched with a finite timeout). The old values were nine orders of magnitude below genuine TSO timestamps, so TiKV could never conclude that an abandoned lock had expired, blocking every later reader of the affected keys. `LockResolver`'s constructor now takes `PdClientInterface $pdClient`, `int $callerStartTs` and `TimeoutConfig $timeoutConfig` (internal collaborator, not public API). (#270)
- `Transaction::commit()` no longer allocates a new commit timestamp when retried after a failed commit phase — the stored commit timestamp is reused, eliminating the risk of a transaction being committed at two different timestamps (partial-transaction visibility window). (#217)
- Fix 3 unit tests that failed in Docker: correct mock expectations for `BatchAsyncExecutor` short-circuit behavior and skip unreadable-file test when running as root. (#178)

### Added
- `bin/pick-issue.sh` — dependency-light ranked issue picker for the milestone workflow (plain bash + `gh` CLI, no PHP/Python/standalone `jq`). Lists open milestones, picks the **lowest** one (semver), scores its open issues (severity `/` type `/` meta labels, title signals, age, comment count) and prints the top N with an explicit per-issue score breakdown (`--top`, `--json`, `--milestone`, `--repo`). Exits with code **3** (RELEASE NEEDED) when the target milestone has no open issues left, enforcing the workflow's release gate. `workflow.md` step 1 now uses it as the fast path, and `composer pick-issue` runs it. (#457)
- `options['allowedStorePorts']` on `RawKvClient::create()`/`TxnKvClient::create()` — optional `list<int>` (or `null` = unrestricted) restricting which ports a PD-supplied store address may use. On the explicit `allowedStoreHosts` path ports are unrestricted unless the option is set, in which case the port must be listed (backward compatible); on the default PD-derived policy path it relaxes or narrows the built-in privileged-port guard (ports `>= 1024`, default `20160+` passes). `storeHostPolicy` (full address) still takes precedence and is never subject to the port policy. (#306)
- `options['allowedStoreHosts']` and `options['storeHostPolicy']` on `RawKvClient::create()`/`TxnKvClient::create()` — host restrictions for store addresses returned by PD: exact hostnames (no wildcard/subdomain match), DNS suffixes (leading dot, e.g. `.tikv.svc`), IPv4/IPv6 CIDR ranges, exact IPv6 addresses (store form `[addr]:port`), or a custom callable policy (receives the full `host:port`, overrides both `allowedStoreHosts` and the default PD-derived policy). A store address outside the allowed set is rejected with a logged `InvalidStoreAddressException`. (#306)
- `RawKvClient::ingest()` — bulk-import key-value pairs into TiKV via SST (Sorted String Table) ingestion. Bypasses the normal Raft write path and directly ingests pre-sorted data into TiKV regions, achieving much higher throughput for large data loads. All TiKV stores are switched to import mode during the operation and switched back to normal mode on completion (even on failure). Supports optional TTL. (#29)
- `SstIngestor` in `src/Client/RawKv/` — handles the SST ingest logic: sorting keys, grouping by region, streaming SST data via the TiKV ImportSST `RawWrite` RPC, and ingesting via the `Ingest` RPC. Uses `try/finally` to ensure stores are always switched back to normal mode. (#29)
- `GrpcClientInterface::callStreaming()` — client-streaming gRPC support for SST import's `Write`/`RawWrite`/`Upload` RPCs. Opens a streaming call, sends multiple request messages, and receives a single response. (#29)
- `PdClientInterface::getAllStores()` — retrieve all TiKV stores from PD, needed for SwitchMode operations during SST ingest. (#29)
- `TimeoutConfig::ingestTimeoutMs` — configurable timeout for SST ingest operations (default: 60 seconds). (#29)
- `RawKvClient::OP_INGEST` — operation type constant for ingest operations. (#29)
- Generated `import_sstpb` and `brpb` protobuf PHP classes from `kvproto`. (#29)
- Unit tests for `SstIngestor` covering: empty input, key sorting, region grouping, SwitchMode lifecycle (import + normal), error recovery (stores switched back on failure), TTL support, multiple stores, and empty store address handling. (#29)
- Unit tests for `RawKvClient::ingest()` covering: client closed validation, empty array, empty key, key size, and value size validation. (#29)

### Added
- `CodecInterface` in `src/Client/Codec/` — defines the contract for key encoding/decoding across TiKV API versions (V1 passthrough, V2 prefix-based). Includes `encodeKey()`/`decodeKey()` for user keys, `encodeRegionKey()`/`decodeRegionKey()` for PD region lookups, `encodeRange()`/`decodeRange()` for key ranges, and `getApiVersion()`/`getKeyspaceId()`/`getKeyspaceName()` metadata accessors. (#28)
- `Mode` enum in `src/Client/Codec/` — Raw/Txn key-space mode used by API V2 key prefix encoding. Provides `prefixByte()` (0x72 for Raw, 0x78 for Txn) and `fromPrefixByte()` factory. (#28)
- `MemComparableCodec` in `src/Client/Codec/` — Memory Comparable Encoding (MCE) for region boundary keys, implementing TiDB's `codec.EncodeBytes`/`DecodeBytes` convention. Escapes 0x00/0xFF bytes with padding and appends a terminator so that encoded keys sort in the same byte-wise order as the original keys. (#28)
- `CodecV1` in `src/Client/Codec/` — V1 codec implementation: keys pass through unchanged, region keys are MCE-encoded only for TxnKV mode (RawKV uses passthrough). Returns `APIVersion::V1`, keyspace ID 0, and empty keyspace name. (#28)
- Unit tests for `MemComparableCodec` (encode/decode round-trips, sort-order preservation, edge cases with null/FF bytes, missing terminator errors) and `CodecV1` (RawKV passthrough, TxnKV MCE region encoding, range encode/decode, empty keys, binary keys, custom MCE injection). (#28)

### Changed
- **Transaction SRP decomposition** — broke up the 944-line `Transaction` god object into three focused collaborators: `TransactionState` (mutable state), `TxnReader` (get/batchGet/scan), and `TwoPhaseCommitter` (commit/rollback/pessimistic lock/heartbeat). The public `Transaction` API is preserved as a thin façade delegating to these classes, following the same decomposition pattern already established in the RawKv module (`RawKvCrud`, `RawKvBatch`, etc.). (#83)

### Added
- `ConnectionFactory` and `ConnectionBundle` in `src/Client/Connection/` — extract the shared TLS parsing, gRPC/PD wiring and timeout-config construction that was duplicated between `RawKvClient::create()` and `TxnKvClient::create()`. Both `create()` methods now delegate to `ConnectionFactory::create()`, eliminating ~100 lines of copy-paste bootstrap code. (#94)
- `RegionGrouper::groupItemsByRegion()` — a generic region grouping method that accepts any item array with a key-extractor callable, allowing `Transaction::groupMutationsByRegion()` to delegate to the shared grouper instead of reimplementing the same batch-resolution loop. (#94)
- `RegionErrorHandler::check()` now accepts optional `RegionCacheInterface $cache` and `?int $regionId` parameters: when provided, the region is invalidated from the cache before the `RegionException` is thrown. `Transaction::handleRegionError()` and `LockResolver::checkTxnStatus()` now delegate to `RegionErrorHandler::check()`, eliminating a latent correctness divergence where the shared handler did not invalidate the cache but the inline copies did. (#94)
- `MetricsInterface` in `src/Client/Observability/` — optional zero-cost observability hook for emitting RPC counts (`rpcStarted`/`rpcCompleted`), retry counts (`retryAttempted`), region-cache hit/miss (`regionCacheHit`/`regionCacheMiss`) and region-invalidation events (`regionInvalidated`). Counters are tagged by operation (e.g. `'tikvpb.Tikv/KvGet'`) or backoff type (`'NotLeader'`, `'ServerBusy'`, …) and the implementation is free to bucket tags however it wishes. The default `NoOpMetrics` is on the hot path of every gRPC call, region lookup and retry, so callers that do not opt in pay a single empty-method dispatch per call site. Pass an implementation via `RawKvClient::create()` and `TxnKvClient::create()` options key `'metrics'`, or inject it directly through the constructor. (#116)
- `InMemoryMetrics` — a thread-unsafe reference implementation useful for tests and benchmarks; counters are stored as `[tag => count]` maps and RPC latency is retained so tests can assert on mean latency or counts. (#116)
- `RawKvClient::healthCheck()` / `TxnKvClient::healthCheck()` — issues the lightweight `GetMembers` RPC (no user data, no "no region" failure) and returns the learned cluster ID, or null if the PD response carried no cluster ID header. Suitable as a load-balancer / Kubernetes readiness probe. (#116)
- `RawKvClient::getMetrics()` / `TxnKvClient::getMetrics()` — accessor for the metrics implementation in use, allowing in-process inspection of counters. (#116)
- `PdClientInterface::ping()` — dedicated `GetMembers` RPC concretely implemented in `PdClient::ping()`; uses the existing cluster-id mismatch retry path. (#116)
- `HealthCheckException` in `src/Client/Exception/` — thrown by `healthCheck()` on transport / PD failure; extends `TiKvException` so it composes with the existing exception hierarchy. (#116)
- Unit tests for `MetricsInterface`/`NoOpMetrics`/`InMemoryMetrics` covering counter independence, mean-latency computation, region-cache and retry tagging, and `reset()` semantics. (#116)
- Unit tests for `RawKvClient::healthCheck()` (cluster ID returned, null when PD has no cluster ID, transport failure becomes `HealthCheckException`, throws `ClientClosedException` after `close()`). (#116)
- Unit tests for `RawKvClient::getMetrics()` (default is `NoOpMetrics`, injected instance round-trip, factory option accepted, factory rejects non-`MetricsInterface` values). (#116)
- Unit tests for `RegionResolver` cache-hit / cache-miss metric emission. (#116)
- Unit tests for `RetryExecutor` metric emission (no-retry yields no metrics, retryable error increments retry counter, attempts under attempt-cap all emit). (#116)
- Unit tests for `PdClient::ping()` (cluster ID from response header, null when missing, propagates gRPC errors). (#116)
- E2E tests `testHealthCheckReturnsLearnedClusterId` / `testHealthCheckAfterClientCloseThrows` / `testMetricsBackendReceivesRpcCountersOnRealOperation` — exercise the new APIs end-to-end against a real PD cluster. (#116)

### Security
- **Plaintext gRPC is no longer the silent default** — `GrpcClient` now logs a warning whenever an insecure (plaintext) channel is opened. A new `allowInsecure` constructor parameter (defaults to `true` for backward compatibility) controls whether insecure channels are permitted; set `allowInsecure: false` to fail-closed when no TLS configuration is provided. (#80)
- **Partial TLS configuration now throws `InvalidArgumentException`** — `TlsConfig` constructor rejects configurations with client certificate/key without a CA certificate. Previously, a partial mTLS configuration (client cert+key without CA) was reported as "not enabled", causing the connection to silently downgrade to plaintext, sending credentials over an unencrypted channel. (#80)
- **`TlsConfig::isEnabled()` now detects any TLS material** — returns `true` when either a CA certificate or a client certificate+key pair is present, preventing partial configurations from being misinterpreted as "disabled". Added `TlsConfig::isComplete()` to check for a fully valid configuration (CA certificate present). (#80)

### Fixed
- **Batch operations now issue all per-region gRPC sends before any wait begins** — `RawKvBatch`'s `batchGetWithRetry`/`batchPutWithRetry`/`batchDeleteWithRetry` previously called `$future->wait()` inside the retry closure, which meant the public `BatchAsyncExecutor::executeParallel` always received already-resolved values and the genuine fan-out path (`executeBatch*ForRegionAsync()` returning un-waited `GrpcFuture`s) was dead code. Each call now returns an un-waited `GrpcFuture` wrapped in `CheckedGrpcFuture`, so every region's gRPC send RPC is dispatched in the executor's dispatch phase (true client-side fan-out at the wire layer) before the executor's wait phase collects responses in declaration order. Region-error checking moves to `CheckedGrpcFuture::waitForExecutor()` and is enforced uniformly for the single-region fast path and the multi-region split/merge slow path. The executor's deadline safeguard (`BatchDeadlineExceededException`) now cancels any in-flight futures when the wall-clock budget is exhausted, preventing gRPC channel/completion-queue leaks. The `slowLog` threshold is still honoured — measurement now wraps the inner wait callback so the logged duration reflects the actual round-trip latency. (#81)
- **TxnKv pessimistic locking & lock resolution are now safe** — `LockResolver::resolveLock()` now accepts a `LockInfo` proto object and extracts the lock's actual `primary_lock` for `CheckTxnStatus`, preventing status checks against the wrong key that could roll back a live transaction. `LockResolver::checkTxnStatus()` now uses the `Action` enum constants (`Action::NoAction`, `Action::MinCommitTSPushed`) instead of the dead `(string) $action === 'Lock'` comparison. `Transaction::pessimisticLockBatch()` no longer swallows `GrpcException` (transport errors now propagate), and on a locked-key conflict the lock is retried with exponential backoff capped by `maxBackoffMs`. `for_update_ts` now uses a fresh PD timestamp (not `startTs`) and the maximum is carried into prewrite. All `resolveLock()` callers (`get()`, `scan()`, `prewrite`, `rollback`) now pass the `LockInfo` object with the correct primary key. (#73)

### Added
- `CheckedGrpcFuture` — lazy region-error-checking wrapper for un-waited `GrpcFuture`s and synthetic message producers (`fromGrpcFuture()` / `fromCallable()`). Used by `RawKvBatch` to defer region-error checking from the dispatch phase to the executor's wait phase while preserving batch fan-out. (#81)
- `BatchDeadlineExceededException` — thrown by `BatchAsyncExecutor::executeParallel($regionCalls, $deadlineMs)` when dispatch + wait exceeds the wall-clock budget. The exception carries the configured deadline, the elapsed time, and diagnostic context (`dispatchedRegions` / `pendingRegions`) describing where in the lifecycle the budget ran out. (#81)
- `BatchAsyncExecutor::executeParallel()` accepts an optional `$deadlineMs` parameter; pass `0` (the default) to disable the deadline entirely. (#81)
- Unit tests for `BatchAsyncExecutor` deadline semantics: dispatch-phase cancellation, wait-phase cancellation, zero-deadline passthrough, `CheckedGrpcFuture` fan-out, region-error-to-partial-failure mapping. (#81)
- Unit tests for `CheckedGrpcFuture`: lazy inner accessor, region-error surface at wait boundary, cancel/no-op for synthetic wrappers. (#81)
- E2E test `testBatchFanOutAcrossMultipleRegions` — writes 600 keys (well above any region split boundary) and reads them back in a single `batchGet`, asserting that the multi-region fan-out returns every value in the original key order. (#81)

### Fixed
- **`Transaction::commitKeys()` now enforces primary-region-first ordering and retries secondary-region commits** — previously, 2PC commit dispatched `KvCommit` RPCs to all regions in `keysByRegion` order with no retry wrapper. A transient region error on any secondary region threw after the primary was already committed, leaving a **half-committed transaction** whose status was never set to Committed. TiKV also rejects secondary commits that arrive before the primary's commit. `commitKeys()` now resolves the primary's region and commits it first (no retry, because re-trying an errored primary commit would invalidate the captured `commitTs`), then sets `TransactionStatus::Committed` immediately on primary success; secondary regions are committed under the existing retry executor (commits are idempotent given a fixed `start_version` + `commit_version`, so transient region/grpc errors are safely retryable). `commitForRegion()` now asserts `commitTs` is non-null before sending `commit_version`. The dead `$firstRegionKeys`/`$isPrimaryRegion` variables that hinted at intended-but-missing primary-first logic have been replaced with explicit primary-region resolution. (#76)
- **Transaction::scan() now enforces a `MAX_SCAN_LIMIT` (10240), applies the limit after write-set merge, and inserts in-range write-set keys not returned by TiKV** — previously, limit=0 sent uint32 max (memory exhaustion risk), and the limit was not re-applied after merging results with the write set. Additionally, newly-written keys inside the scan range that TiKV had not yet committed were silently missing from the result, violating read-your-writes semantics for scans. (#86)
- **RawKvBatch retry now re-groups keys after region split/merge** — `resolveRegion()` no longer returns a stale `RegionInfo` when the region ID changes; the retry wrappers (`batchGetWithRetry`, `batchPutWithRetry`, `batchDeleteWithRetry`) now verify every key still falls within the resolved region's range and, when a split/merge scatters keys across multiple regions, re-resolve and re-dispatch each sub-group to its own region. `RegionErrorHandler::check()` now surfaces per-pair `KeyError`s from `RawBatchGetResponse` and top-level error strings from `RawBatchPutResponse`/`RawBatchDeleteResponse`, eliminating silent partial writes/deletes/reads. (#140)
- **`__destruct()` on `RawKvClient`, `TxnKvClient`, and `Transaction`** — clients now close deterministically when dropped without an explicit `close()` call; `Transaction` rolls back if still active, preventing leaked server-side locks. Destructors never throw. (#91)
- **`RegionCache` now bounded with LRU eviction and incremental index updates** — `RegionCache` grew without bound and had O(n²) warm-up cost from rebuilding the `idToIndex` map on every insertion/removal. Added configurable `maxEntries` (default 10 000) with LRU eviction; `idToIndex` is now updated incrementally instead of rebuilt from scratch; periodic expired-entry sweep runs every N puts (configurable `sweepInterval`, default 100). `clear()` is now called in `RawKvClient::close()` and `TxnKvClient::close()`. (#82)
- **`PdClient ↔ TimestampOracle` reference cycle broken** — `TimestampOracle` now receives `getClusterId`/`setClusterId` closures instead of the full `PdClient` reference, allowing the cycle collector to reclaim memory promptly without relying on GC. (#91)

### Added
- `SlowLogConfig` — optional, injectable configuration for slow operation logging in `RawKvClient`. Accepts per-operation-type thresholds (read, write, batch read/write, scan, delete range, checksum) in milliseconds. Pass via `RawKvClient::create()` options key `'slowLog'`. When a gRPC call exceeds its threshold, a PSR-3 warning is logged with the redacted key, duration, and threshold. (#31)
- `TimeoutConfig` now has a `checksumTimeoutMs` property (default 30s) for the `RawChecksum` RPC, separate from `deleteRangeTimeoutMs`. Configured via `'timeout'` option key `checksumTimeoutMs`. (#31)
- Unit tests for `SlowLogConfig`: default values, custom values, per-operation threshold lookups, unknown operation returns zero, zero threshold disables logging. (#31)
- `StoreCache` now has a `maxEntries` cap (default 128) with LRU eviction — when at capacity, the least recently used entry is evicted before inserting a new one, preventing unbounded memory growth over time. (#108)
- `GrpcClient` `closed` guard — `close()` sets an internal flag and `call()` throws `InvalidStateException` if invoked after close, making the closed state explicit and preventing latent use-after-close bugs. (#108)
- Unit tests for `StoreCache` LRU eviction: eviction at capacity, correct LRU ordering with multiple entries, overwrite does not count towards capacity, clear resets and allows re-adding. (#108)
- Unit tests for `PdClient::close()`: verifies store cache is cleared, cluster ID is reset, and TSO is nulled; verifies the shared `GrpcClient` is NOT closed by `PdClient`. (#108)
- Unit test for `GrpcClient::call()` throwing `InvalidStateException` after `close()`. (#108)
- Channel eviction in `GrpcClient`: idle channels are closed after a configurable TTL (default 10 min); at most `maxChannels` (default 64) are cached, evicting the least recently used channel when at capacity; channels in `TRANSIENT_FAILURE` or `SHUTDOWN` states are now reaped on next access (previously only `FATAL_FAILURE` was reaped). (#89)
- `__destruct()` on `GrpcClient` — closes all cached channels when the reference is dropped without an explicit `close()` call. (#89)
- `GrpcResponseParser::setMaxMessageSize()` — configurable maximum protobuf message size guard before `mergeFromString()`, preventing potential DoS from oversized messages (defense in depth for CVE-2026-6409). Default is 0 (unlimited). (#75)
- Unit tests for `GrpcResponseParser` max message size: within limit, exceeds limit, null message, zero disables limit. (#75)
- `TlsConfigBuilder::withCaCertFile()`, `withCaCertPem()`, `withClientCertFile()`, `withClientCertPem()` — explicit API to distinguish file paths from inline PEM content, replacing the ambiguous `file_exists()` guessing in legacy methods. The new methods support an optional `$baseDir` parameter to restrict allowed directories. The legacy `withCaCert()` and `withClientCert()` are deprecated. (#87)
- `RegionRangeClipper` in `src/Client/Region/` — centralises the region range-clipping logic that was duplicated across `RawKvScanner`, `RawKvRangeOps` and `Transaction`. All three call sites now delegate to the shared clipper, ensuring consistent half-open `[start, end)` semantics and empty-end-key = +infinity treatment. (#84)
- Unit tests for `RegionRangeClipper` covering forward/reverse clipping across multiple adjacent regions, empty end key (+infinity), keys at region split points, range outside region, and empty regions array. (#84)
- Unit tests for `RawKvScanner` multi-region scan boundary clipping, empty-end-key unbounded scan, three-region limit spanning, reverse-scan limit across regions, all-0xFF prefix to empty-end-key conversion, and non-aligned key-range clipping at region split points (#85)
- Exposed cluster ID from PD via `RawKvClient::getClusterId()` and `TxnKvClient::getClusterId()`, delegating to `PdClientInterface::getClusterId()` (#27)
- Atomic mode for Compare-And-Swap (CAS): CAS and Put-If-Absent now require `setAtomicForCAS(true)` to be called first, enabling the TiKV `for_cas` atomic code path. Disabled by default for performance. (#103)
- Dedicated CI lane for gRPC-dependent unit tests with the PHP gRPC extension loaded and skipped tests failing (#117)
- E2E tests for client lifecycle: Close then get/put/delete/scan/... throws ClientClosedException, double close is idempotent, transaction before close remains usable (#55)
- Removed `__construct()` from `GrpcClientInterface` — interfaces should not enforce constructor signatures; all consumers use DI or concrete class instantiation (#46)
- `RegionInfoMapper`, a shared proto→DTO mapper used by `PdClient::getRegion()` and `scanRegions()` so both RPCs share identical, explicit null-handling for a missing leader (#104)
- Unit tests for `RetryExecutor` budget exhaustion (total backoff, ServerBusy, max-attempts cap, wall-clock deadline), `BatchAsyncExecutor` dispatch-phase vs wait-phase failure aggregation, `TlsConfigBuilder` disallowed-extension rejection, and `TlsConfig::close()` key-zeroing (#100)

### Changed
- **`PdClient::close()` no longer closes the shared `GrpcClient`** — ownership of the gRPC connection pool belongs to the high-level client (`RawKvClient` / `TxnKvClient`), which already closes it. `PdClient::close()` now only clears its own resources: store cache, cluster ID, and TSO. This prevents double-close on the shared `GrpcClient`. (#108)
- **Upgraded `google/protobuf` from `^3.25` to `^4.33.6`** — fixes CVE-2026-6409 (HIGH), a DoS vulnerability through malicious protobuf messages containing negative varints or deep recursion. (#75)
- `composer.json` `config.audit.block-insecure` set to `true` — vulnerable dependencies now fail `composer audit` and block CI. (#75)
- **Moved shared region DTOs and helpers from `RawKv` to `Region` namespace**: `RegionInfo` and `PeerInfo` DTOs moved to `CrazyGoat\TiKV\Client\Region\Dto`, `RegionGrouper` and `RegionErrorHandler` moved to `CrazyGoat\TiKV\Client\Region`. All imports updated across the codebase. The old `RawKv\Dto\RegionInfo`, `RawKv\Dto\PeerInfo`, `RawKv\RegionGrouper`, and `RawKv\RegionErrorHandler` aliases are removed. (#111)
- Merged `commitOptimistic()` and `commitPessimistic()` into a single `doCommit()` method; removed dead `$firstRegionKeys`/`$isPrimaryRegion` that were never used (#113)
- **Error classification now uses typed `ErrorKind` enum instead of message-string matching.** Added `ErrorKind` enum covering all 21 `Errorpb\Error` oneof variants; `RegionException` auto-detects the kind from the proto `Error` message; `ErrorClassifier::classifyByKind()` is the single source of truth for the error→backoff mapping; `Transaction::classifyError()` uses `TxnRetryableException` carrying `BackoffType` directly; `PdClient::extractClusterIdFromError()` uses pure regex without `str_contains()` (#93)

### Fixed
- **`pessimisticRollbackAll()` now uses the actual `for_update_ts` from the pessimistic lock phase** — previously it always sent `startTs` as `for_update_ts` in `PessimisticRollbackRequest`, but `pessimisticLockBatch()` acquires locks with a fresh PD timestamp stored in `TransactionState::maxForUpdateTs`. When these differ, TiKV ignores the rollback request and leaves dangling pessimistic locks that block concurrent transactions for up to 30 seconds. The rollback now reads `$state->getMaxForUpdateTs() ?? $this->startTs`, matching the timestamp used during lock acquisition. (#176)
- **Security**: TLS file path inputs are now canonicalized with `realpath()` and validated against an optional allowed base directory, preventing path traversal via `../` and symlink attacks. Exception messages no longer leak the resolved filesystem path. The legacy `withCaCert()` and `withClientCert()` methods that guess between file path and inline PEM via `file_exists()` are deprecated in favor of the new explicit `*File()` and `*Pem()` methods. (#87)
- Removed `instanceof PdClient` concrete-class checks from `TimestampOracle`; added `setClusterId()` to `PdClientInterface` so any implementation of the interface can propagate cluster-ID discovery (DIP/LSP compliance) (#110)
- Replaced bare `\RuntimeException` and `\LogicException` with `InvalidStateException` (extends `TiKvException`) in `RawKvClient::compareAndSwap()`, `Transaction::ensureActive()`, `Transaction::getPrimaryKey()`, and `GrpcClient::createTlsCredentials()`, so all library failures derive from `TiKvException` (#102)
- TxnKv RPCs now have per-operation deadlines via `TimeoutConfig`, threaded from `TxnKvClient::create()` `options['timeout']` into `Transaction`, preventing indefinite blocking on hung TiKV nodes (#79)
- Added `@throws` annotations to all public methods in `RawKvClient` and `Transaction`, documenting the typed exceptions they raise (`InvalidArgumentException`, `ClientClosedException`, `InvalidStateException`, `RegionException`, `GrpcException`, `BatchPartialFailureException`, `TransactionConflictException`, `DeadlockException`) (#102)
- `PdClient::getRegion()` no longer fabricates `regionId=0`/`leaderStoreId=1` when PD returns no region/leader — it now throws a `TiKvException` on a missing region and reports leader store id `0` ("unknown") for a missing leader, so `resolveStoreAddress()` raises a visible `StoreNotFoundException` instead of silently routing requests to a guessed store on split/merge (#104)
- `GrpcFuture` now exposes `cancel()` and `__destruct()` so abandoned pending gRPC calls are cancelled instead of leaking completion-queue/channel resources; `BatchAsyncExecutor` cancels remaining un-waited futures on the first wait-phase failure (#90)
- `splitPairsIntoBatches()` now normalizes `$pairs` and `$ttls` keys to sequential 0-indexed arrays, preventing silent TTL misalignment when pairs have non-sequential keys (#106)
- Removed dead runtime guard in `batchScan()` that was suppressed with `@phpstan-ignore`; the PHPDoc type `array<array{0: string, 1: string}>` already guarantees the contract, and malformed input now produces a `TypeError` instead of `InvalidArgumentException` (#112)
- `getKeyTTL()` now validates key (empty/oversized), consistent with `get()`, `put()`, `delete()` (#109)
- `deletePrefix()` now rejects prefixes consisting entirely of 0xFF bytes, which would previously delete to the end of the keyspace instead of just the intended range (#105)
- Replaced `array_merge` in scan loops with `array_push` to avoid O(n²) copying across regions (#97)
- `getPrimaryKey()` now uses `array_key_first()` instead of `array_keys()`, avoiding O(n) memory allocation on every call (#114)
- PHPStan analysis now sets an explicit memory limit of 1G, preventing crashes with the default 128M limit (#115)
- Raw keys are now automatically redacted in log context across `RegionCache`, `RetryExecutor`, `Transaction`, and `LockResolver` — added `KeyRedactor` utility that replaces raw keys with a hex prefix + length, preventing sensitive-data leakage into log aggregation. A custom redaction callable can be injected via `KeyRedactor::setRedactor()`. (#88)
- `get()`, `batchGet()`, and `Transaction::get()` now correctly distinguish empty-string values from missing keys by checking the response's `not_found` flag instead of treating empty values as null (#77)
- `batchPut()` with a scalar TTL now expands the TTL to one element per pair instead of sending a 1-element array for an N-key batch, ensuring every key receives the intended expiry (#78)
- `LockResolverTest` now executes (all 11 tests) instead of erroring on every method — replaced `createMock()` of generated protobuf messages with real `CheckTxnStatusResponse`/`ResolveLockResponse` instances constructed via setters, restoring coverage of the commit/rollback/wait/region-error decision matrix (#99)
- `close()` is now exception-safe across `GrpcClient`, `RawKvClient`, and `TxnKvClient` — a throw from one channel/sub-client no longer leaks the rest or leaves the client in a non-idempotent state (#107)
- `RetryExecutor::execute()` is now bounded by a configurable attempt cap (`maxAttempts`, default 30) and an optional wall-clock deadline (`deadlineMs`), independent of accumulated backoff. Previously, errors classified as `BackoffType::None` (e.g. `EpochNotMatch`) returned `sleepMs=0`, so `totalBackoffMs` never grew and the `while (true)` loop drove an infinite zero-sleep 100%-CPU busy loop. New exhaustion throws `RetryBudgetExhaustedException` (extends `TiKvException`) (#72)
- Corrected `CHANGELOG.md`, `README.md`, and `docs/configuration.md` to reference actual APIs, constants, and file paths instead of nonexistent ones (`GrpcClient::setTimeout()` → `TimeoutConfig`/`options['timeout']`, `MAX_RAW_SCAN_LIMIT` → `MAX_SCAN_LIMIT`, removed dead `RegionContext.php` and `docs/superpowers/` link, moved shipped "In Progress" features to "Recently Completed", clarified env var usage as application-level wiring) (#101)

## [0.2.0] - 2026-05-11

### Added
- TxnKV client with full ACID transaction support (begin/commit/rollback, snapshot reads, pessimistic locking)
- Lazy auto-paginating `ScanIterator` for RawKV scans — fetches next pages transparently on iteration
- Auto-split large batches by key count and total byte size — prevents oversized gRPC requests
- Configurable per-operation gRPC timeouts via `TimeoutConfig` / `options['timeout']`
- Scan limit guard (`MAX_SCAN_LIMIT = 10240`) — prevents accidental unbounded scans
- Per-key TTL support in `batchPut()` — accepts `int|array $ttl` for individual key expiration times
- E2E tests running against a real TiKV cluster in CI via GitHub Actions

### Removed
- `docs/superpowers/` — all implementation plans migrated to GitHub issues

## [0.1.0] - 2026-03-30

### Added
- Initial release of TiKV PHP Client
- Complete RawKV operations support:
  - Single-key operations: Get, Put, Delete
  - Batch operations: BatchGet, BatchPut, BatchDelete with parallel execution
  - Scanning: Scan, ScanPrefix, ReverseScan, BatchScan
  - Range operations: DeleteRange, DeletePrefix
  - TTL support: PutWithTTL, GetKeyTTL
  - Atomic operations: CompareAndSwap, PutIfAbsent
  - Data integrity: Checksum
- Production features:
  - TLS/SSL support (server verification and mTLS)
  - PSR-3 logging integration
  - Automatic retry with exponential backoff
  - Region and store caching
  - Connection pooling via persistent gRPC channels
- Comprehensive documentation:
  - Getting Started guide
  - Configuration reference
  - Operations guide
  - Advanced patterns
  - Troubleshooting guide
  - Contributing guide
  - Development guide
  - Architecture documentation
- Working examples for all major features
- Full test suite: 148 unit tests + 141 E2E tests
- CI/CD pipeline with GitHub Actions

### Infrastructure
- Branch protection rules requiring CI checks and approvals
- PHP 8.2, 8.3, 8.4 support

[0.2.0]: https://github.com/crazy-goat/tikv-php/releases/tag/v0.2.0
[0.1.0]: https://github.com/crazy-goat/tikv-php/releases/tag/v0.1.0
